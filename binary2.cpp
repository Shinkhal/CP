//array of n length where array of i is always greater than zero , q is querries array =[l,r] where l,r denotes startng ad ending points and then decrement by 1

#include<bits/stdc++.h>
using namespace std;

int main(){
    int arr[5] = {1,1,1,1,1};
    int n = sizeof(arr)/sizeof(arr[0]);
    int q[4][2];
    q[0][1] = 0; 
    q[1][3] = 1; 

}